spring:
  application:
    name: EmbededConfigServerExampl
  profiles:
    active: dev
  cloud:
    config:
      server:
        git:
          uri: file:///${user.home}/config-repo
        bootstrap: true